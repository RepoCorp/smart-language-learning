Generate adjective exercise phrases for one vocabulary item.

Return strict JSON with this exact shape:
{
  "phrases": [
    {"label": "predicative", "source_text": "string", "target_text": "string"},
    {"label": "attributive", "source_text": "string", "target_text": "string"},
    {"label": "comparative", "source_text": "string", "target_text": "string"},
    {"label": "question", "source_text": "string", "target_text": "string"}
  ]
}

Rules:
- Return exactly 4 phrases, one for each listed label, in the listed order.
- Keep every phrase short (max 6 words), practical, and beginner-friendly (A1-A2).
- The predicative phrase should use the adjective after a linking verb.
- The attributive phrase should use the adjective with a simple noun when natural.
- The comparative phrase should use a basic comparison when natural.
- The question phrase should be a short useful question.
- Besides the target adjective, use only very basic high-frequency words.
- Keep source_text and target_text equivalent in meaning.
- Use the language mapping provided by the user input.
- Return JSON only, no markdown and no extra text.
