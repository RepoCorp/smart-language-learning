Generate noun exercise phrases for one vocabulary item.

Return strict JSON with this exact shape:
{
  "phrases": [
    {"label": "singular", "source_text": "string", "target_text": "string"},
    {"label": "plural", "source_text": "string", "target_text": "string"},
    {"label": "indefinite", "source_text": "string", "target_text": "string"},
    {"label": "dative", "source_text": "string", "target_text": "string"},
    {"label": "accusative", "source_text": "string", "target_text": "string"}
  ]
}

Rules:
- Return exactly 5 phrases, one for each listed label, in the listed order.
- Keep every phrase short (max 6 words), practical, and beginner-friendly (A1-A2).
- Each target_text must clearly practice its label.
- Never return only the noun by itself or only article + noun, such as "der Tisch" or "die Tische"; every item must be a real phrase with context.
- Singular and plural forms should use the nominative case, the noun with its article should start the sentence.
- Cover nominative, dative, and accusative uses.
- Cover definite and indefinite article forms.
- For plural indefinite in German, use natural bare plural forms when there is no indefinite article.
- Besides the target noun, use only very basic high-frequency words.
- Keep source_text and target_text equivalent in meaning.
- Use the language mapping provided by the user input.
- Return JSON only, no markdown and no extra text.
